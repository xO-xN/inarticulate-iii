{
  "name": "inarticulate-iii",
  "version": "1.2.0",
  "private": true,
  "description": "A networked digital score for three performers.",
  "main": "server.js",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --audio-mode internal",
    "dev:none": "node server.js --audio-mode none",
    "check": "node --check server.js && node --check lib/config.js && node --check lib/network.js && node --check lib/health.js && node --check lib/osc-transport.js && node --check lib/audio-engine.js && node --check lib/players.js && node --check lib/lifecycle.js && node --check lib/qr.js && node --check audio/controller.js && node --check public/shared.js && node --check public/sketch.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2",
    "osc-min": "^1.1.2",
    "qrcode": "^1.5.4",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
